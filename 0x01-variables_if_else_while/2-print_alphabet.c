c = 'a'; /* initialize c with the first lowercase letter */
while (c <= 'z') /* loop until the last lowercase letter */
{
    putchar(c); /* print the current letter */
    c++; /* increment c to the next letter */
}
putchar('\n'); /* print a newline character */

return (0);
